# Cores to AVOID per board, i.e. too slow to hold full speed. Only produces a
# warning (log + on-screen message), never blocks the game. Add what you observe.
#
# Format:  <board> : core, core, ...
# <board> is the content of /boot/boot/batocera.board (bcm2837 = Pi 3, bcm2711 = Pi 4).

bcm2837 : snes9x, bsnes, swanstation, beetle_psx, beetle_psx_hw, mupen64plus_next
